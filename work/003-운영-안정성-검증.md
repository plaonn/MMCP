# 운영 안정성 검증

## 상태

진행 중

## 문제

현재 macOS 운영 구성은 기능 검증과 단기 재시작 검증만 완료됨. 장기간 운영,
절전, 네트워크 단절, 인증서 갱신, 업데이트 실패 상황의 안정성은 아직 확인하지
않음.

## 원하는 동작

MMCP가 장기간 실행과 일반적인 장애 상황에서 자동 복구되며, 안전하게 업데이트
및 백업할 수 있어야 함.

## 범위

### 포함

- 1~2주 장기 실행 중 CPU, 메모리, 저장 공간 사용량 관찰
- macOS 절전, 재부팅, 네트워크 단절 후 자동 복구 검증
- OAuth token refresh와 Let's Encrypt 인증서 자동 갱신 검증
- 업데이트, 재시작, 실패 시 복구 절차 작성
- `.env`와 OAuth signing secret 백업 및 복구 절차 작성

### 제외

- 기능 추가
- Raspberry Pi 이전

## 완료 조건

- [ ] 장기 실행과 장애 복구 결과를 기록함.
- [x] 업데이트 및 복구 절차를 문서화함.
- [x] 비밀값 백업 및 복구 절차를 문서화함.

## 결정

- 2026-06-13부터 14일 동안 장기 실행 상태를 관찰함.
- 영구 application/access log를 추가하지 않고 `launchctl`, `ps`, `du`, local
  health endpoint와 Caddy config validation으로 상태를 확인함.
- 현재 checkout 업데이트는 검증, build, MMCP 재시작과 health retry를 하나의
  스크립트로 수행함.
- 비밀값 backup은 `umask 077`로 만든 별도 디렉터리에 저장하고 source control에
  넣지 않음.

## 미결 사항

- 없음

## 구현 참고

- 개인 데이터와 비밀값을 측정 로그에 기록하지 않음.
- 장기 관찰 결과에는 서비스 상태, PID, uptime, CPU, RSS와 디렉터리 크기만
  기록함.

## 관찰 기록

### 2026-06-13 시작 baseline

- MMCP launchd service: running
- Caddy launchd service: running
- local health: `{"status":"ok"}`
- MMCP uptime: 17시간 12분, RSS: 53,264 KiB
- Caddy uptime: 17시간 12분, RSS: 26,656 KiB
- `dist`: 456 KiB, `~/.config/mmcp`: 240 KiB
- 관찰 종료 예정: 2026-06-27

## 검증

- [x] 관련 자동화 테스트를 통과함.
- [ ] 필요한 수동 또는 통합 검증을 완료함.
- [x] `docs/SPEC.md`가 구현 동작과 일치함.
- [x] 필요한 장기 결정이 `docs/decisions/`에 기록됨.

검증 결과:

- `deploy/check-macos-services.sh`가 비밀값 없이 서비스 상태, resource, health와
  Caddy config 상태를 출력함.
- `deploy/update-macos-service.sh`가 typecheck, 전체 86개 테스트, build,
  launchd 재시작과 health retry를 순서대로 완료함.
- 최종 업데이트 검증 직후 MMCP PID는 `85723`, local health와 Caddy config는
  정상이며 `dist`는 456 KiB, `~/.config/mmcp`는 240 KiB임.
- ChatGPT에서 OAuth 재연결 없이 `get_mail_rules`, `check_connection`,
  `search_mail_actions`, `get_mail_action` 조회가 정상 동작함.
- rules revision `24`, IMAP `connected: true`를 확인함.
- 조회한 action의 event 이력은 `created`, `upserted`, `upserted`, `upserted`,
  `updated` 순서였으며 생성 시각 역행이 없었음.
- 상태 변경 도구와 이메일 본문·첨부파일·원본 조회 도구는 호출하지 않음.
